// Configurações dos diretórios das pastas

const { resolve } = require('path')

module.exports = {
    config: resolve( __dirname, 'src', 'config', 'database.js'), // onde está os arquivos de configuração do sequelize
    'models-path': resolve(__dirname, 'src', 'app', 'models'),   // onde está localizado os models
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations')  // onde está localizado as migrations
}